@model IEnumerable<Books.Models.Book>

@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <li>
            @item.AuthorName
        </li>
        <li>
            @item.BookName
        </li>
    }
}
else
{
    <h3 class="text-error">Sorry! No items matched your search!</h3>
}
